# FutureLinked ZA Environment Variables

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# OpenAI API Configuration (Optional but recommended for AI features)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Google AdSense (Optional)
NEXT_PUBLIC_GOOGLE_ADSENSE_ID=

# Google Analytics (Optional)
NEXT_PUBLIC_GA_ID=

# Instructions:
# 1. Copy this file and rename it to '.env.local'
# 2. Replace 'your_openai_api_key_here' with your actual OpenAI API key
# 3. Restart your development server after adding the key
# 4. Without OpenAI key, the system will use rule-based analysis (still works well)
